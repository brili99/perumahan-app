workflows:
  my-workflow:
    scripts:
      - name: Build ipa for distribution
        script: xcode-project build-ipa \
                    --workspace "Runner.xcworkspace" \
                    --project "Runner.xcodeproj" \
                    --scheme "perumahan-app-test"
    artifacts:
      - build/ios/ipa/*.ipa
